name = "chefleads"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Secrets and vars are provided via the Cloudflare dashboard in production and
# via .dev.vars locally (see .env.example). Do not commit real secrets here.
