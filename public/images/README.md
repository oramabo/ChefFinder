# Brand images

Drop the generated photos here with these exact filenames. They're referenced
as `/images/<name>` from the app (Vite serves `public/` at the site root).

| Filename | Which generated image | Used on | Suggested size |
|---|---|---|---|
| `hero.jpg` | Chef's hands plating the Levantine dish (landscape) | Homepage hero | ~1600×1067 (3:2) |
| `og.jpg` | Candlelit dinner table at dusk (landscape) | Social share card **and** homepage closing band | 1200×630 (1.91:1) |
| `pomegranate.jpg` | Split pomegranate, seeds spilling (vertical) | Homepage "why" card | ~900×1100 |
| `olive.jpg` | Olive branch + thyme on linen (landscape) | "For chefs" page banner | ~1600×700 (16:7) |
| `saffron.jpg` | Saffron + spices in a ceramic bowl (square) | Spare accent (not placed yet) | ~1000×1000 |

Notes:
- **JPG or WebP** are both fine; if you use WebP, rename the references or tell me
  and I'll switch the extensions. Keep each file roughly < 300 KB (export quality
  ~80) so pages stay fast.
- `og.jpg` should be close to **1200×630** for clean link previews on
  WhatsApp/Facebook; the same image also backs the homepage closing band.
- Until the files are added, those spots show a blank/broken image — everything
  else renders normally.
